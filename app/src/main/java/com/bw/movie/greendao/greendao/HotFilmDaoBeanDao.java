package com.bw.movie.greendao.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.bw.movie.greendao.HotFilmDaoBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "HOT_FILM_DAO_BEAN".
*/
public class HotFilmDaoBeanDao extends AbstractDao<HotFilmDaoBean, Long> {

    public static final String TABLENAME = "HOT_FILM_DAO_BEAN";

    /**
     * Properties of entity HotFilmDaoBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property FollowMovie = new Property(0, String.class, "followMovie", false, "FOLLOW_MOVIE");
        public final static Property Id = new Property(1, long.class, "id", true, "_id");
        public final static Property ImageUrl = new Property(2, String.class, "imageUrl", false, "IMAGE_URL");
        public final static Property Name = new Property(3, String.class, "name", false, "NAME");
        public final static Property Rank = new Property(4, int.class, "rank", false, "RANK");
        public final static Property Summary = new Property(5, String.class, "summary", false, "SUMMARY");
    }


    public HotFilmDaoBeanDao(DaoConfig config) {
        super(config);
    }
    
    public HotFilmDaoBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"HOT_FILM_DAO_BEAN\" (" + //
                "\"FOLLOW_MOVIE\" TEXT," + // 0: followMovie
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 1: id
                "\"IMAGE_URL\" TEXT," + // 2: imageUrl
                "\"NAME\" TEXT," + // 3: name
                "\"RANK\" INTEGER NOT NULL ," + // 4: rank
                "\"SUMMARY\" TEXT);"); // 5: summary
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"HOT_FILM_DAO_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, HotFilmDaoBean entity) {
        stmt.clearBindings();
 
        String followMovie = entity.getFollowMovie();
        if (followMovie != null) {
            stmt.bindString(1, followMovie);
        }
        stmt.bindLong(2, entity.getId());
 
        String imageUrl = entity.getImageUrl();
        if (imageUrl != null) {
            stmt.bindString(3, imageUrl);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(4, name);
        }
        stmt.bindLong(5, entity.getRank());
 
        String summary = entity.getSummary();
        if (summary != null) {
            stmt.bindString(6, summary);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, HotFilmDaoBean entity) {
        stmt.clearBindings();
 
        String followMovie = entity.getFollowMovie();
        if (followMovie != null) {
            stmt.bindString(1, followMovie);
        }
        stmt.bindLong(2, entity.getId());
 
        String imageUrl = entity.getImageUrl();
        if (imageUrl != null) {
            stmt.bindString(3, imageUrl);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(4, name);
        }
        stmt.bindLong(5, entity.getRank());
 
        String summary = entity.getSummary();
        if (summary != null) {
            stmt.bindString(6, summary);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 1);
    }    

    @Override
    public HotFilmDaoBean readEntity(Cursor cursor, int offset) {
        HotFilmDaoBean entity = new HotFilmDaoBean( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // followMovie
            cursor.getLong(offset + 1), // id
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // imageUrl
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // name
            cursor.getInt(offset + 4), // rank
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5) // summary
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, HotFilmDaoBean entity, int offset) {
        entity.setFollowMovie(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setId(cursor.getLong(offset + 1));
        entity.setImageUrl(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setName(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setRank(cursor.getInt(offset + 4));
        entity.setSummary(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(HotFilmDaoBean entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(HotFilmDaoBean entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(HotFilmDaoBean entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
